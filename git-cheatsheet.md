# Git 指令速查表

## 日常提交

- `git status`：查看目前狀態。
- `git diff`：查看尚未暫存的修改。
- `git add <file>`：將修改加入暫存區。
- `git commit -m "訊息"`：建立提交。

## 分支操作

- `git switch -c <branch>`：建立並切換分支。
- `git switch <branch>`：切換分支。
- `git merge <branch>`：合併指定分支。

## 遠端同步

- `git fetch origin`：取得遠端資訊但不合併。
- `git pull`：取得並合併遠端更新。
- `git push`：推送本機提交。
